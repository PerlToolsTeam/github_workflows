name: YAML Linting

on:
  workflow_call:

jobs:
  yaml-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install yamllint
        run: |
          pip install yamllint
      - name: Run yamllint
        run: |
          yamllint .
